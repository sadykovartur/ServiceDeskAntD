# ЛР8 — Admin + качество + CI/Docker

## Admin
- Categories CRUD: Table + Modal create/edit + enable/disable
- Users & roles: список пользователей + смена роли

## Инженерное
- Docker compose: db + api (+ опц. client)
- CI: server build+test, client lint+build
- Server tests: smoke + 1–2 интеграционных на auth/роль

## Сдать
PR, CI зелёный, проект поднимается по README.


> Подсказка: в шаблоне уже есть `.github/workflows/ci.yml`.
